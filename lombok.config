# Lombok adds @Generated to all generated code (used to ignore in NullAway)
lombok.addLombokGeneratedAnnotation = true

# Lombok copies @Nullable to generated constructors/setters
lombok.copyableAnnotations += org.jspecify.annotations.Nullable